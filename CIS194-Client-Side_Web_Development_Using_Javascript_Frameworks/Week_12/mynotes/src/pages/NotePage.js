import React from 'react'

const NotePage = (props) => {
    // let noteId = match.params.id;
    console.log(props)
  return (
    <div>
      <h1>props.match</h1>
    </div>
  )
}

export default NotePage
